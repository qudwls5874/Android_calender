<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 파란색 테두리를 가진 동그라미 모양의 배경 -->
    <item>
        <shape
            android:shape="oval">
            <solid android:color="@android:color/transparent" />
            <stroke
                android:width="1dp"
                android:color="@color/textPlusColor" />
        </shape>
    </item>
</layer-list>
