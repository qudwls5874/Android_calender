<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- 상단, 좌측, 우측 테두리 설정 -->
    <solid android:color="@android:color/transparent" /> <!-- 배경색은 투명으로 설정 -->
    <stroke
        android:width="1dp"
        android:color="@color/BackBaseInputBoxColor" /> <!-- 테두리 두께와 색상 -->
</shape>
